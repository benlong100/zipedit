build/
vendor/merlin32/
vendor/*.po
vendor/*.dsk
tools/ac
tools/acx
tools/ac-mac-*
tools/acx-mac-*
tools/merlin32
tests/snapshots/*.actual.png
.DS_Store
notes/

# The web page lives with the rest of the trompingmarmots.com sites, not here.
# This repository is the editor. The built disk image is published as a release
# asset instead, so the download still has a permanent URL.
web assets/
web/
tools/mksite.py
